{
  "name": "alaboud-business-suite-cloud",
  "version": "18.5.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend --no-audit --no-fund && npm install --prefix frontend --no-audit --no-fund",
    "build": "npm run build --prefix frontend && node scripts/copy-frontend.js",
    "start": "node backend/src/server.js",
    "render-build": "npm run install:all && npm run build",
    "selftest": "node backend/src/selftest_v8.js"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
