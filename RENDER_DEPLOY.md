# Render v8.2
Root Directory: اتركه فارغًا

Build Command:
```text
npm run render-build
```

Start Command:
```text
npm start
```

Health Check Path:
```text
/api/health
```

بعد نسخ الملفات إلى مستودع GitHub:
```powershell
git add .
git commit -m "AlAboud v8.2 customers and capital"
git push
```
ثم Manual Deploy → Deploy latest commit.
