# تحسين الأداء — المرحلة الأولى

- الشاشات الثقيلة مفصولة داخل `frontend/src/screens/` وتُحمّل باستخدام `React.lazy` عند فتحها.
- تحديث لوحة التحكم يتوقف تلقائيًا عندما تكون الصفحة في الخلفية.
- سجل أسعار الصرف في لوحة التحكم يطلب آخر 50 سجلًا فقط.
- مسار الخادم لسجل أسعار الصرف يفرض حدًا افتراضيًا 50 وحدًا أقصى 200.
- Chromium يُثبّت تلقائيًا لتشغيل موصل جاد على Render.
- لم تُعدّل قاعدة PostgreSQL أو بيانات العملاء.
