# AlAboud Business Suite v25.14.122 — Clean Full Source

هذه الحزمة مبنية من مرجع v25.14.121 مع دمج إصلاحات v25.14.122 وإصلاحات Cloud Build/Docker-safe التي ثبتت أثناء النشر.

## إصلاح الدفعات
- مهلة GET: 45 ثانية.
- مهلة POST/PUT/PATCH/DELETE: 30 ثانية.
- عند انقطاع الرد بعد إرسال عملية مالية لا يعاد إرسال POST تلقائياً.
- يتم التحقق من نفس `Idempotency-Key` بفواصل 500، 1000، 1500، 2500، 4000 مللي ثانية.
- COMMITTED/SUCCESS/COMPLETED = نجاح.
- FAILED/REJECTED/ROLLED_BACK = فشل مؤكد.
- UNKNOWN/PENDING/PROCESSING = استمرار تحقق محدود فقط.
- إذا بقيت الحالة مجهولة تظهر رسالة تطلب تحديث حساب العميل قبل إعادة المحاولة.
- زر حفظ الدفعة محمي من النقر المكرر ويظل معطلاً طوال الحفظ/التحقق.
- حماية مسار الدفعة من تحديث React بعد unmount.

## إصلاحات الإصدار والبناء
- Version: 25.14.122
- Android versionName: 25.14.122
- Android versionCode: 2514122
- Android CLIENT_VERSION: 25.14.122
- `check:current` يشير إلى `version-consistency-v2514122.test.js`.
- اختبار version consistency يتعامل بأمان مع استبعاد `.github` و`.env.example` من Docker/Cloud Build.
- احتُفظ بإصلاحات v25.14.121: Multi-Currency، Treasury backup، WhatsApp، Voice Phase 2، security، PostgreSQL، Production Readiness.

## الفحوص المنفذة على هذه الحزمة
- `npm run check:sensitive`: PASS (775 files scanned قبل تنظيف node_modules الجزئي).
- `npm run check:current`: PASS.
- `npm run check:reliability`: PASS.
- `npm run check:regressions`: PASS.
- اختبارات إصلاح الدفعة المركزة: PASS.
- version consistency v25.14.122: PASS.

ملاحظة: لم تُضمّن `node_modules` أو ملفات build الناتجة داخل الحزمة. التثبيت الصحيح يكون عبر `npm run install:all` ثم `npm run check:release` في بيئة متصلة بـ npm. تعذر إكمال `npm ci` داخل بيئة تجهيز الملف بسبب مهلة اتصال الشبكة، وليس بسبب فشل في كود المشروع.
