# تحسين وتقسيم المشروع v24.0.5

## ما تم تنفيذه

- خفض حجم `frontend/src/App.jsx` من نحو 148 KB إلى نحو 24 KB.
- فصل لوحة التحكم إلى `screens/Dashboard.jsx` وتحميلها عند فتح الرئيسية فقط.
- فصل قائمة العملاء والعملاء المتأخرين إلى `screens/Customers.jsx`.
- فصل ملف العميل والفاتورة وكشف الحساب إلى `screens/CustomerDetails.jsx`.
- فصل صفحة الحوالات إلى `screens/Transactions.jsx`.
- الإبقاء على جميع الصفحات الثقيلة باستخدام `React.lazy` و`Suspense`.
- إزالة نسخة تسجيل الدخول المكررة من `App.jsx` والاعتماد على `LoginShell.jsx`.
- حذف ملفات توافق وشاشات قديمة غير مستخدمة: LazyScreens وProfits وMonthlyReport وأغلفة صفحات الشركات غير المستخدمة.
- تحسين مترجم اللغة: لم يعد يفحص كامل DOM بعد كل تغيير؛ أصبح يعالج العناصر الجديدة فقط باستخدام `requestAnimationFrame`.
- عرض ملخص لوحة التحكم أولاً، وتأجيل البيانات الثانوية إلى وقت خمول المتصفح، مع إلغاء الطلب المكرر للملخص.
- توحيد الإصدار إلى 24.0.5 في الواجهة والخادم وAndroid وحزم npm.

## النتيجة المتوقعة

- تحميل أولي أخف، خصوصاً على الهاتف.
- انتقال أسرع بين الصفحات لأن كل شاشة تُحمّل عند الحاجة.
- تقليل استهلاك المعالج الناتج عن مراقبة وترجمة الصفحة بالكامل.
- ظهور لوحة التحكم الأساسية قبل التقارير والبحث والبيانات الثانوية.
- صيانة أسهل وأقل احتمالاً لتعارض التعديلات داخل ملف واحد ضخم.

## الفحوصات

نجح فحص صياغة الخادم، واختبار الصلاحيات، واختبار الترحيل، والتحقق من جميع مسارات الاستيراد المحلية.
تعذر تشغيل بناء Vite الكامل لأن مستودع npm في بيئة الفحص أعاد خطأ 404 لحزمة `yallist`; لذلك يجب أن ينفذ GitHub Actions أو Render بناء الواجهة النهائي.
