# v25.14.84 — توحيد حساب دين الشركات

## الهدف
إزالة الاختلاف بين «الدين العام ← دين الشركات لنا» وصفحة الشركات، وربط الميزانية والجرد بالمعادلة نفسها من دون أي تغيير في موصلات الشركات أو المزامنة.

## المعادلة المركزية
تمت إضافة `backend/src/finance/CompanyDebtPosition.js` كمصدر حساب موحّد:

1. تُجمع حركات الشركة المحلية لكل عملة على حدة.
2. يحسب الرصيد الصافي المحلي للعملة: `لنا - علينا - دفعات مستلمة + دفعات مدفوعة`.
3. إذا كان الناتج موجبًا يصنف «دين لنا»، وإذا كان سالبًا يصنف «دين علينا».
4. الأرصدة الخارجية متعددة العملات تبقى منفصلة كما يعيدها الموصل، مع حماية منع التكرار المرتبط بـ `sourceRef`.
5. التحويل بين العملات يستخدم `currencyConversion` الافتراضي الذي يعتمد أسعار الصرف الآلية فقط.

## الأسطح التي تستخدم الدالة نفسها
- `/api/partners` — صفحة الشركات.
- `/api/general-debts` — الدين العام ونافذة دين الشركات لنا/علينا.
- `/api/capital-overview` — الميزانية.
- `calculateInventoryPosition` / الجرد الشهري.

## الحماية من تغيير الموصلات
تمت مقارنة مصدر v25.14.84 مباشرة بمصدر v25.14.83:
- كتلة JAD من `syncJadPartnerHttp` حتى بداية حساب أرصدة الشركات: متطابقة حرفيًا.
- كتلة `resolvePartnerConnector` وما يليها من موصلات ومزامنة حتى endpoint تفاصيل الشركة: متطابقة حرفيًا.
- لا توجد أسطر diff مضافة أو محذوفة تحمل JAD/TAWASUL/DAHAB/SURYANA أو دوال مزامنتها.

## الاختبارات المنفذة
- `npm run check:v251484` — ناجح.
- `npm run check:regressions` — ناجح.
- `npm run check:reliability` — ناجح.
- `backend/src/finance/MonthlyInventoryFinancials.test.js` — ناجح بعد تحديث التوقعات للمعادلة الموحّدة.
- Backend sweep: **111/115 ناجحة**. الاختبارات الأربعة المتبقية محجوبة محليًا لغياب حزمة `pg`/عدم بدء الخادم بسبب الاعتماديات غير المثبتة، وليست إخفاقات حسابية.
- اختبارات الواجهة الثابتة نجحت؛ اختباران يحتاجان `vite` محجوبان محليًا لأن الاعتماديات لم تُثبت في بيئة العمل.

## ملاحظة النشر
Dockerfile يثبت الاعتماديات من lock files داخل Cloud Build ثم يشغل `check:v251484` و`check:reliability` و`check:regressions` قبل بناء الواجهة. يوصى بنشر revision مع `--no-traffic` واختباره قبل تحويل حركة الإنتاج.
