# v25.14.37 — إصلاح الميزانية والجرد الشهري

- إصلاح خطأ `ReferenceError: customerBalanceTotals is not defined` في `/api/capital-overview`.
- إصلاح نفس الخطأ في `/api/monthly-inventory`.
- ربط `customerBalanceTotals` مباشرة من `FinancialEngine` قبل استخدامه في مسارات الميزانية والجرد.
- لا يغيّر هذا الإصلاح معادلات الدين أو صافي رأس المال؛ بل يعيد تفعيل الحسابات المصنفة في v25.14.35.
- إضافة اختبار يمنع تكرار فقدان الربط مستقبلاً.
