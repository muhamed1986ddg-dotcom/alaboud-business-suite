# v25.14.18 — استعادة عمليات التعديل والحذف

يعالج هذا الإصدار بقاء طلبات `transactions` في حالة Pending عند تعطل اتصال PostgreSQL أو انتظار قفل كتابة.

- مهلة انتظار الاتصال من Pool محدودة.
- مهلة انتظار القفل داخل PostgreSQL محدودة.
- مهلة تنفيذ جملة الكتابة محدودة على الخادم وعلى عميل Node.js.
- فحص إيصال Idempotency قبل العملية أصبح سريعًا ولا يدخل في حلقة استعادة طويلة.
- فحص نتيجة العملية بعد انقطاع الاستجابة أصبح محدود الزمن.
- لا يتم التخلي عن COMMIT المتين ولا عن منع تكرار العملية.
