# تحقق الإصدار v25.14.80

## الحالة

- إصدار الويب والخادم: `25.14.80`.
- غلاف Android: `25.14.74` — لم يتغير في هذا الإصدار.
- لا يوجد migration لقاعدة البيانات.
- لم يُنشر الإصدار ولم تُحوّل إليه أي حركة إنتاجية.

## الحسابات التي تم تثبيتها

- `14,684.22 − 7,184.43 = 7,499.79 CAD` لصافي الربح المسجل.
- `36,249.03 + 7,499.79 = 43,748.82 CAD` لصافي رأس المال في حالة الاختبار.
- `146,629.50 − 13,933.08 = 132,696.42 CAD` لصافي الذمم، دون فرق سنت.
- مكونات الجرد تُقرب إلى سنت CAD قبل تركيب قيمة الجرد وفرق المطابقة.
- سجلات الجرد التاريخية الكاملة يعاد تركيب عرضها عند القراءة فقط، دون تعديل السجل المخزن.

## نتائج التحقق

- الاختبارات الرقمية الجديدة للحساب والجرد: `4/4` ناجحة.
- اختبارات فصل واجهة الميزانية: `4/4` ناجحة.
- الاختبارات الخلفية الشاملة: `108/108` ناجحة.
- اختبارات الواجهة الشاملة: `15/15` ملف اختبار ناجح.
- اختبارات مكونات الواجهة: `14/14` ناجحة.
- فحص الملفات الحساسة: ناجح، `618` ملفًا مفحوصًا.
- بناء Vite الإنتاجي: ناجح، `102` وحدة محولة.
- نسخ البناء إلى `backend/public`: ناجح.

تم تشغيل مكونات بوابة الإصدار كلٌ على حدة. تعذر تشغيل أمر التجميع `npm test` نفسه داخل بيئة المراجعة بسبب إلغاء موافقة الشبكة الخاصة بالأداة، لكن الاختبارات التي يجمعها الأمر شُغلت مباشرة ونجحت كما هو موضح أعلاه.

## نقطة تعريف لم تتغير

«الربح المسجل» حاليًا يعني ربح كل حوالة غير محذوفة وغير ملغاة. لم يُغيّر هذا الإصدار لحظة الاعتراف بالربح إلى وقت التحصيل؛ لأن ذلك يحتاج قرارًا تشغيليًا منفصلًا.

## أسلوب الاختبار السحابي لاحقًا

استخدم `DEPLOY_CLOUD_RUN_V25_14_80.ps1` فقط بعد نسخ المشروع إلى جهاز Windows. ينشئ السكربت Revision معزولًا باستخدام `--no-traffic` ولا يحول حركة الإنتاج.
