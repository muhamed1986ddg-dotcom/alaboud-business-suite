# تقرير النسخة النظيفة v25.14.123

## الميزة الجديدة
- زر «💬 إرسال المجموع النهائي» في بطاقة كل عميل ضمن صفحة العملاء.
- الرسالة لا ترسل كشف الحساب ولا الحوالات ولا الدفعات؛ ترسل الرصيد النهائي فقط.
- يستخدم الرصيد النهائي الموجود أصلًا في سجل العميل دون أي معادلة مالية جديدة.
- اتجاه الرصيد يظهر تلقائيًا: «عليكم» للرصيد الموجب، «لكم» للرصيد السالب، و«لحسابكم» للصفر.
- رقم WhatsApp يُقرأ من whatsapp ثم mobile ثم phone حسب المتاح.

## قالب WhatsApp
- تمت إضافة إعداد مستقل: `finalBalanceWhatsAppTemplate`.
- يمكن تعديله من إعدادات WhatsApp تحت «قالب إرسال المجموع النهائي».
- القالب الافتراضي:

  السلام عليكم {customerName}

  المجموع النهائي {balanceDirection}:
  {balance} CAD

  أبو إسلام

- المتغيرات المدعومة: `{customerName}`, `{name}`, `{balance}`, `{balanceAbs}`, `{signedBalance}`, `{balanceDirection}`, `{currency}`.

## سلامة الحسابات
- لم تتغير معادلات الرصيد أو الحوالات أو الدفعات أو الخزنة.
- الزر للقراءة/الإرسال اليدوي فقط ولا ينشئ POST ماليًا.
- جميع إصلاحات v25.14.122 الخاصة بتأكيد الدفعات ومنع التكرار محفوظة.

## الإصدار
- Backend / Frontend / Root: 25.14.123
- Android versionName: 25.14.123
- Android versionCode: 2514123

## الفحوص المنفذة في بيئة التجهيز
- `npm run check:current`: PASS
- `npm run check:reliability`: PASS
- `npm run check:regressions`: PASS
- `npm run check:sensitive`: PASS (749 ملفًا)
- اختبار الميزة الجديدة Frontend: PASS
- اختبار ربط الميزة Backend/Settings: PASS
- Notification routes regression: PASS
- Version consistency v25.14.123: PASS
- فحص syntax لملفات Backend المعدلة: PASS

الاختبارات التي تتطلب حزم `vite`/`pg` لم تُشغل بالكامل داخل الحزمة النظيفة لأنها لا تحتوي `node_modules`. يجب تشغيل `npm run install:all` ثم `npm run check:release` قبل النشر الفعلي.
