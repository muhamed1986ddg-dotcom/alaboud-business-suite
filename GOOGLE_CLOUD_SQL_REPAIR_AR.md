# إصلاح اتصال Google Cloud SQL

تم تعديل طبقة PostgreSQL لتقليل أخطاء `Query read timeout` و`57014` تحت الضغط.

## ما تغير
- رفع مهلة الاستعلام الافتراضية من 12 ثانية إلى 30 ثانية.
- رفع مهلة إنشاء الاتصال إلى 20 ثانية.
- رفع حدود معاملة الحفظ المالي المتزامن بحيث لا تُلغى عملية صحيحة بسبب بطء مؤقت.
- عدم التخلص من اتصال PostgreSQL تلقائياً عند `57014` فقط؛ هذا الكود يعني غالباً أن PostgreSQL ألغى الاستعلام بسبب timeout وليس أن socket مات.
- الإبقاء على relational mirror معطلاً افتراضياً لأنه ليس مصدر الحقيقة ويضيف حملاً كبيراً أثناء الحفظ.
- إضافة ملف `cloudrun-postgres-recommended.yaml` بالقيم المقترحة لـ Cloud Run.

## مهم
مصدر الحقيقة يبقى `app_state`، ولا يوجد في هذا الإصلاح أمر حذف بيانات أو DROP/TRUNCATE.
