# تقرير الإصدار v25.12.0

## الإصلاحات المنفذة
- إنشاء حاسبة مركزية مستقلة `ReceivableSummary` لحساب الدين لنا والدين علينا وصافي الدين.
- تثبيت قاعدة العمل: **الدين لنا = ديون العملاء + ديون الشركات فقط**.
- إبقاء الديون اليدوية ظاهرة للمراجعة دون إضافتها إلى المؤشر الرئيسي.
- استبدال اختبار البحث عن النصوص باختبارات رقمية فعلية، ومنها الحالة 68,245.69 + 72,589.22 = 140,834.91 CAD.
- إضافة كشف للتشابه بين اسم العميل واسم جهة الدين اليدوي.
- منع إنشاء قيد يدوي مطابق لاسم عميل دون تأكيد صريح، لتقليل خطر العد المكرر.
- رفع الحد التقني لمسار خيارات العملاء من 500 إلى 5000، وإضافة دعم paging اختياري للمسار.
- تحديث رقم الإصدار في الحزم والواجهة إلى 25.12.0.

## الفحوصات الناجحة
- فحص صياغة ملفات الخادم الأساسية.
- اختبارات الحسابات الرقمية للدين.
- اختبار اتساق v25.12.0 المالي.
- اختبار Pagination للعملاء.
- اختبار تطبيع أسماء الجهات المتشابهة.
- فحص الملفات الحساسة: 213 ملفًا دون أسرار مكتشفة.
- فحص أساس واجهة المستخدم.

## قيود بيئة الفحص
تعذر تشغيل Vite build واختبار الواجهة الكامل لأن مستودع الحزم في بيئة الفحص أعاد 404 للحزمة yallist@3.1.1. لذلك لم يتم الادعاء بأن Build الواجهة أو E2E المتصل بمتصفح نجح داخل هذه البيئة.

## ملاحظات إنتاجية باقية
- JWT ما زال محفوظًا في localStorage؛ نقله إلى HttpOnly Cookie يحتاج ترحيل جلسات متكامل واختبار CSRF وAndroid WebView قبل اعتماده.
- الملفات الكبيرة `server.js` و`styles.css` و`Customers.jsx` ما زالت تحتاج تقسيمًا تدريجيًا.
- قائمة اختيار العميل في الواجهة ما زالت تحمل دفعة أولية؛ المسار أصبح يدعم paging لكن تحويل عناصر الاختيار إلى بحث لحظي يحتاج تعديل واجهة مستقل.
- يلزم نجاح CI على GitHub/Render، واختبار PostgreSQL حقيقي، وBuild Android Release قبل وصف النسخة بأنها Production Ready نهائيًا.

## التصنيف الصادق
هذه النسخة **مرشح إنتاج قوي** من ناحية الإصلاح المالي المحدد والفحوصات الساكنة والرقمية المنفذة. الاعتماد الإنتاجي النهائي مشروط بنجاح CI، Build الواجهة، PostgreSQL integration، وAndroid Release في بيئة النشر.
