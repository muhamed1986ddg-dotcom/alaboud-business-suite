# v25.14.83 — إصلاح 429 وتحديث أسعار الصرف

## الإصلاحات

- Android يعترض HTTP 429 في الإطار الرئيسي ويعرض شاشة عربية بدل الصفحة البيضاء.
- إعادة الاتصال من شاشة 429 محدودة بمحاولتين فقط.
- مصدر أسعار العملات ومصدر الذهب يُجلبان بالتوازي باستخدام `Promise.allSettled`.
- جميع الأسعار الناجحة تُحفظ في عملية `mutateDurable` واحدة بدل 27 كتابة متتابعة.
- أزيل التحديث الساعي من كل تبويب متصفح؛ التحديث الساعي المركزي في الخادم بقي كما هو.
- عند فشل مصدر أو الحفظ، لا تُحذف الأسعار السابقة وتوضح الرسالة أن آخر أسعار صحيحة محفوظة بقيت دون تغيير.
- لم يتم تعديل منطق أو ملفات ربط Jad أو Tawasul أو Dahab أو Suryana. وبالمقارنة مع v25.14.82، `server.js` متطابق خارج كتلة تحديث أسعار الصرف ومسارها.

## الاختبارات المنفذة

- `npm run check:v251483` — ناجح.
- `npm run check:reliability` — ناجح؛ اختبار SIGTERM التشغيلي فقط سجّل skip لأن حزم التشغيل غير مثبتة محليًا.
- `npm run check:regressions` — ناجح بالكامل.
- `node --check backend/src/server.js` — ناجح.
- فحص حد حجم `server.js` — 6221 سطرًا، ضمن الحد 6250.

## ملاحظة بيئة العمل

تعذر إكمال `npm ci` المحلي بسبب انتهاء مهلة تنزيل الحزم في بيئة العمل، لذلك لم يُنفذ build محلي كامل للواجهة. Dockerfile يعيد تثبيت الحزم ويبني الواجهة داخل Cloud Build، كما أُضيف `check:v251483` إلى بوابة بناء Docker قبل النشر.
